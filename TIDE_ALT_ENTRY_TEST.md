# Tide 山寨币两种入场对照（2026-09-09）

按用户纠正后的目标市场：读取固定研究分支 v10_bundle/stage2_full_results.csv 的 eligible=True 标的，排除 BTCUSDT/ETHUSDT/SOLUSDT。核对原表234个eligible中不含这三个币，因此计划测试234个山寨币。

运行：

```bash
python backtest_tide_alt_entries.py --symbols ALT_BUNDLE --days 100 --end 2026-09-09T00:00:00Z --out /data/tide_alt_entries
```

保持 v2 两个可比路径的定义：共同原始形态与Raw>=58，early下一根开盘；confirmed等待下一根收盘且Next>=95、Combined>=70、确认>=2/3后再下一根开盘。没有历史评分、等级、Rescue或完整线上环境过滤。两组都使用原始低点−0.5原始ATR止损和自入场最多4小时固定退出。不是完整线上策略复刻。

本金1000U；固定10U计划风险；2500U名义上限；成本0.10%；最多12仓/60U开放风险/800U保证金参考/悉尼日40U累计亏损。新增资金可负担性保护：新仓所需总保证金或总计划风险不能超过剩余已实现权益。对两组相同，避免账户耗尽后继续模拟交易；不是优化信号。跳空可使实际亏损超过计划，未模拟爆仓和盘中浮亏。

4个有界数据工作线程，仅处理独立标的，不访问生产状态或发通知。每个标的输入连续性、预热和截止日期相同标准校验；缺失数据对两组共同排除并标为INCOMPLETE。不得把失败标的当零交易。

本轮是当前标的池回看历史，存在选币与存活者偏差；不能称为独立OOS。不搜索阈值，不将最赚钱币种事后升级为新策略。原始事件、候选交易及完整汇总压缩后以分块日志输出，退出后可重建；单行headline去掉大体积逐币细节。
